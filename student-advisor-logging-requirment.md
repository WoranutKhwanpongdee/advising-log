# AdvisingLog: ระบบบันทึกการเข้าพบอาจารย์ที่ปรึกษา (สาขา SE)

> เอกสารความต้องการฉบับแรกของโปรเจกต์ AdvisingLog — สร้างเมื่อ 2026-08-26, ปรับปรุงเมื่อ 2026-08-26 (5 รอบ)
> สถานะ: ผู้ใช้ยืนยันประเด็นเปิดทั้ง 3 ข้อแล้ว (โมเดลลายเซ็น, การเชื่อมต่อ reg, PDPA/สิทธิ์เข้าถึงบันทึกอ่อนไหว —
> ดูสรุปผลที่หัวข้อ 7) ให้รายละเอียดฟิลด์ระดับ AUN-QA เพิ่มเติมแล้ว (ดู FR-23–FR-25 และ mapping ที่หัวข้อ 8)
> เพิ่มความต้องการอีเมลสรุป+ลิงก์ตอบกลับสำหรับการ route คำขออัตโนมัติ (ดู FR-26) เพิ่มการเก็บ root
> cause ของ dropout ในขอบเขต A เท่านั้น — ไม่รวม narrative synthesis/NLP (ดู FR-27, FR-28, หัวข้อ 7.4)
> และยืนยันสิทธิ์การเข้าถึงข้อมูล case-level ของเคส dropout แบบ scoped carve-out พร้อม access audit log
> (ดู FR-20, FR-28, NFR-06, NFR-07, หัวข้อ 7.5)

## 1. บทนำ / ความเป็นมา

สาขา Software Engineering (SE) ต้องการระบบ **AdvisingLog** เพื่อเก็บข้อมูลการที่นักศึกษาขอเข้าพบอาจารย์ที่ปรึกษา
โดยมีเป้าหมายหลักเพื่อใช้เป็นหลักฐานตอบ **AUN-QA** ดังนี้:

- **Criterion 6 (Student Support Services)** เป็นหลัก
  - **6.2** — การติดตามความก้าวหน้าของนักศึกษา (monitoring student progress)
  - **6.3** — ความพร้อมและคุณภาพของการให้คำปรึกษา (advising availability & quality)
- เชื่อมโยงกับ **Criterion 5 (Staff Quality)** — ภาระงาน (workload) ของอาจารย์ที่ปรึกษา
- เชื่อมโยงกับ **Criterion 8 (Output/Outcomes)** — ข้อมูลสนับสนุนการวิเคราะห์ retention/dropout

### 1.1 บริบท/flow ปัจจุบัน (paper-based)

1. นักศึกษาเปิดระบบ reg ของมหาวิทยาลัย → ตรวจสอบชื่ออาจารย์ที่ปรึกษาของตนเอง
2. กรอกฟอร์มกระดาษเพื่อขอเข้าพบ
3. นัดอาจารย์ที่ปรึกษาทางอีเมลเพื่อขอลายเซ็น

**ปัญหา:** ข้อมูลกระจัดกระจายอยู่ในฟอร์มกระดาษและอีเมล ไม่สามารถรวบรวมเป็นสถิติเพื่อจัดทำหลักฐาน AUN-QA ได้

### 1.2 เป้าหมายของระบบใหม่

แทนที่ paper-based flow ด้วยระบบออนไลน์ที่ครอบคลุมตั้งแต่การยื่นคำขอ, การนัดหมาย, การบันทึกผลการพบ,
การอนุมัติ/ลายเซ็น, การประเมินความพึงพอใจ ไปจนถึง dashboard สถิติสำหรับใช้เป็นหลักฐาน AUN-QA

## 2. ขอบเขต (Scope)

### 2.1 อยู่ในขอบเขต (In scope)

- การยื่นคำขอเข้าพบออนไลน์ พร้อมแนบเอกสาร
- การนำเข้า (import) และดูแล roster นักศึกษา–อาจารย์ที่ปรึกษาภายในระบบเอง โดย Admin (เนื่องจากเชื่อมต่อกับ
  ระบบ reg ของมหาวิทยาลัยโดยตรงไม่ได้ — ยืนยันแล้ว ดูหัวข้อ 7.2)
- การ route คำขอไปยังอาจารย์ที่ปรึกษาที่ถูกต้อง โดยอ้างอิง roster นักศึกษา–อาจารย์ที่ปรึกษาที่ดูแลอยู่ภายในระบบ
- การนัดหมายและแจ้งเตือนภายในระบบ
- การบันทึกผลการพบ (interaction note) และการติดตามผล (outcome/follow-up)
- การอนุมัติ/ลงนามเอกสารแบบผสม (hybrid): อัปโหลดเอกสารที่เซ็นจริงบนกระดาษ (สแกน) เป็น path หลัก
  และลงลายมือชื่ออิเล็กทรอนิกส์ (e-signature) ในระบบสำหรับประเภทเอกสารที่อนุญาตเท่านั้น พร้อม audit trail
  ทั้งสอง path (ยืนยันแล้ว ดูหัวข้อ 7.1)
- แบบประเมินความพึงพอใจสั้นๆ หลังการพบ
- การวัดเวลาตอบสนอง (ยื่น → ได้นัด) อัตโนมัติ
- Dashboard สถิติและการ export หลักฐานสำหรับ AUN-QA Criterion 6 (อ้างอิง C5, C8)
- การจัดการสิทธิ์ผู้ใช้ตามบทบาท

### 2.2 ไม่อยู่ในขอบเขต (Out of scope)

- การเชื่อมต่อ (integrate) กับระบบ reg ของมหาวิทยาลัยแบบ real-time หรือผ่าน API — ยืนยันแล้วว่าระบบ reg
  ไม่เปิดให้เชื่อมต่อ ระบบ AdvisingLog จึงดูแล roster นักศึกษา–อาจารย์ที่ปรึกษาของตัวเอง แทนการดึงข้อมูล
  จาก reg โดยตรง (ดู FR-04, FR-22, NFR-02 และหัวข้อ 7.2)
- ผู้ให้บริการ/มาตรฐาน e-signature ที่เฉพาะเจาะจงสำหรับเอกสารประเภท e-signature-allowed — ยังไม่ตัดสินใจ
  จนกว่าจะถึงขั้น technology-stack

## 3. บทบาทผู้ใช้ (Actors)

| บทบาท | คำอธิบาย |
|---|---|
| นักศึกษา (Student) | ยื่นคำขอเข้าพบ, ดูสถานะ/ประวัติคำขอของตนเอง, ทำแบบประเมินความพึงพอใจ |
| อาจารย์ที่ปรึกษา (Advisor) | รับคำขอที่ระบบ route มาให้, นัดหมาย, บันทึกผลการพบ, อนุมัติ/ลงลายมือชื่อ |
| หัวหน้าสาขา/ผู้ประสาน AUN-QA (Program Chair / QA Coordinator) | ดู dashboard สถิติรวมของสาขา, export หลักฐานสำหรับ AUN-QA, ดูภาระงาน (workload) ของอาจารย์ |
| ผู้ดูแลระบบ (Admin) | จัดการสิทธิ์ผู้ใช้/บทบาท, ตั้งค่าประเภทเรื่อง (category) และวิธีลงนามที่อนุญาตต่อประเภท, นำเข้า/ดูแล roster นักศึกษา–อาจารย์ที่ปรึกษา, ดูแล audit trail ของระบบ |

## 4. ความต้องการเชิงฟังก์ชัน (Functional Requirements)

| รหัส | ความต้องการ | รายละเอียด | ระดับความสำคัญ |
|---|---|---|---|
| FR-01 | ยื่นคำขอเข้าพบออนไลน์ | นักศึกษายื่นคำขอเข้าพบอาจารย์ที่ปรึกษาผ่านระบบ เลือกประเภทเรื่อง และแนบเอกสารประกอบได้ แทนฟอร์มกระดาษ (รองรับ C6.3) | สูง |
| FR-02 | ประเภทเรื่อง (category) มาตรฐาน — dropdown บังคับ 9 ประเภท | ระบบบังคับให้นักศึกษาเลือกประเภทเรื่องจาก **dropdown ที่กำหนดไว้ล่วงหน้า (mandatory, ไม่ใช่ free text)** เพื่อให้ aggregate/จัดกลุ่ม/หา trend ได้ตามหลัก AUN-QA รองรับอย่างน้อย 9 ประเภท พร้อม sub-type ย่อยที่เป็น coded value เช่นกัน ดังนี้: (1) เซ็นเอกสารทุน/ทุนการศึกษา, (2) การเงิน [sub-type: ผ่อนผัน, ค้างชำระ], (3) ลงทะเบียน [sub-type: เพิ่ม-ถอน, ลงทะเบียนล่าช้า, เกินหน่วยกิต], (4) รักษาสภาพ, (5) คืนสภาพ, (6) ลาออก/พักการศึกษา, (7) ให้คำปรึกษาผลการเรียน (GPA/probation), (8) ฝึกงาน/สหกิจ/อาชีพ, (9) ส่วนตัว/สุขภาพใจ (ส่งต่อแนะแนว) — รายการนี้ขยายต่อได้ผ่าน Admin (ดู FR-03) หากพบเรื่องที่ยังไม่มีประเภทรองรับ แทนการปล่อยเป็นช่อง "อื่นๆ" แบบ free text แต่ละประเภทเรื่อง/เอกสารมี attribute กำกับวิธีลงนามที่อนุญาต (ดู FR-21) — เมื่อเลือกประเภทเรื่อง (6) ลาออก/พักการศึกษา หรือคำขอถูกระบุว่าเป็นกรณี dropout ระบบต้องบังคับให้กรอก reason code เพิ่มเติมด้วย (ดู FR-27) | สูง |
| FR-03 | จัดการประเภทเรื่อง | Admin สามารถเพิ่ม/แก้ไข/ปิดใช้งานประเภทเรื่องเพิ่มเติมจากรายการมาตรฐานได้ รวมถึงตั้งค่า/แก้ไข attribute วิธีลงนามที่อนุญาตของแต่ละประเภทเรื่อง/เอกสาร (ดู FR-21) | กลาง |
| FR-04 | จัดเก็บ roster นักศึกษา–อาจารย์ที่ปรึกษาภายในระบบ | ระบบเก็บและยืนยันความสัมพันธ์นักศึกษา–อาจารย์ที่ปรึกษาจาก roster ที่ดูแลอยู่ภายในระบบเอง — **ยืนยันแล้วว่าระบบ reg ของมหาวิทยาลัยไม่เปิดให้เชื่อมต่อ** จึงไม่ใช่การดึงข้อมูล/integrate จาก reg แบบ real-time roster นี้ได้มาจากการนำเข้าไฟล์ตาม FR-22 (ดูหัวข้อ 7.2) | สูง |
| FR-05 | Route คำขอไปยังอาจารย์ที่ปรึกษาอัตโนมัติ | ระบบส่งคำขอไปยังอาจารย์ที่ปรึกษาที่ถูกต้องของนักศึกษาผู้ยื่นโดยอัตโนมัติ โดยอ้างอิงความสัมพันธ์จาก roster ภายในระบบ (FR-04/FR-22) แทนที่นักศึกษาต้องค้นหาชื่ออาจารย์เองในระบบ reg — เมื่อ route สำเร็จ ระบบต้องส่งอีเมลสรุปคำขอพร้อมลิงก์ตอบกลับแบบปลอดภัยไปยังอาจารย์ที่ปรึกษาโดยอัตโนมัติด้วย (ดูรายละเอียดที่ FR-26) | สูง |
| FR-06 | นัดหมายภายในระบบ | อาจารย์ที่ปรึกษาเสนอ/ยืนยันวันเวลานัดหมายในระบบ นักศึกษายืนยันการนัด แทนการนัดผ่านอีเมล | สูง |
| FR-07 | แจ้งเตือนอัตโนมัติ | ระบบแจ้งเตือนทั้งสองฝ่ายเมื่อมีคำขอใหม่, มีการยืนยัน/เปลี่ยนแปลงนัดหมาย, และก่อนถึงเวลานัดหมาย (ช่องทางแจ้งเตือนเป็นรายละเอียดเชิงเทคนิคที่ตัดสินใจภายหลัง) — หมายเหตุ: การแจ้งเตือนคำขอใหม่ไปยังอาจารย์ที่ปรึกษาทางอีเมลแบบมีลิงก์ตอบกลับที่บันทึก log การตอบสนองได้ ถูกระบุรายละเอียดเพิ่มเติมเฉพาะเจาะจงไว้ใน FR-26 (เป็นกรณีเฉพาะของการแจ้งเตือน "มีคำขอใหม่" ในข้อนี้) | สูง |
| FR-08 | บันทึกผลการพบ (interaction note) | อาจารย์ที่ปรึกษาบันทึกสรุปเรื่องที่พูดคุย, ปัญหาที่นักศึกษาพบ, และข้อแนะนำที่ให้ไว้ หลังการพบทุกครั้ง (รองรับ C6.2, C6.3) | สูง |
| FR-09 | บันทึกผลลัพธ์และการติดตาม (outcome/follow-up) | ระบบให้บันทึกว่าต้องนัดติดตามต่อหรือไม่, ส่งต่อไปยังหน่วยงานอื่นหรือไม่ (เช่น แนะแนว/ทุน/สุขภาพจิต) พร้อมกำหนดวันติดตาม (รองรับ C6.2) — เมื่อเป็นเคสลาออก/พักการศึกษา/dropout ผลลัพธ์นี้ใช้ร่วมกับ reason code (FR-27) เพื่อประกอบชุดหลักฐานเคส (FR-28) | สูง |
| FR-10 | ส่งต่อไปยังหน่วยงานอื่น (referral) | เมื่อบันทึกว่ามีการส่งต่อในบันทึกผลการพบ ระบบบันทึกหน่วยงานปลายทางที่ส่งต่อ (แนะแนว/ทุน/สุขภาพจิต/อื่นๆ) เพื่อใช้ในการติดตามและสถิติ | กลาง |
| FR-11 | อัปโหลดเอกสารสแกนพร้อม audit trail (wet signature) | อาจารย์ที่ปรึกษา/นักศึกษาอัปโหลดเอกสารที่ต้องเซ็นจริงบนกระดาษ (wet signature) แล้วสแกนเข้าระบบ โดยระบบบันทึกผู้ upload และ timestamp ไว้เป็น audit trail — เป็น **path หลัก** สำหรับประเภทเอกสารที่กำหนดวิธีลงนาม = wet-signature-only (ดู FR-21) — ยืนยันแล้วว่ายังต้องคงไว้เพราะเอกสารจำนวนมากยังต้องเซ็นจริง (ดูหัวข้อ 7.1) | สูง |
| FR-12 | ลายมือชื่ออิเล็กทรอนิกส์ในระบบ (e-signature) | ระบบรองรับการลงลายมือชื่ออิเล็กทรอนิกส์ในระบบเพื่ออนุมัติคำขอ/บันทึกการพบ **เฉพาะประเภทเอกสารที่กำหนดวิธีลงนาม = e-signature-allowed เท่านั้น** (ดู FR-21) ไม่ใช้แทนที่ FR-11 สำหรับเอกสารที่ยังต้อง wet-signature พร้อม timestamp และ audit trail (ดูหัวข้อ 7.1) | กลาง |
| FR-13 | แบบประเมินความพึงพอใจ | ระบบส่งแบบประเมินความพึงพอใจสั้นๆ ให้นักศึกษากรอกหลังการพบทุกครั้ง เพื่อใช้วัดคุณภาพการให้คำปรึกษา (รองรับ C6.3) | สูง |
| FR-14 | วัดเวลาตอบสนองอัตโนมัติ | ระบบบันทึกและคำนวณระยะเวลาตั้งแต่ยื่นคำขอจนถึงได้รับการนัดหมายที่ยืนยันแล้วโดยอัตโนมัติ ไม่ต้องคำนวณด้วยมือ | สูง |
| FR-15 | Dashboard สถิติ | ระบบมี dashboard สำหรับหัวหน้าสาขา/ผู้ประสาน AUN-QA แสดงจำนวนครั้งการพบ (แยกตามประเภทเรื่อง), เวลาตอบสนองเฉลี่ย, คะแนนความพึงพอใจเฉลี่ย, และภาระงาน (workload) ต่ออาจารย์ที่ปรึกษา (รองรับ C6.2, C6.3, C5) — เพิ่มการนับจำนวนเคสลาออก/พักการศึกษา/dropout แยกตาม reason code (FR-27) เป็นระดับสถิติรวม (นับจำนวน ไม่ใช่เนื้อหาบันทึกดิบ — ดู FR-20/NFR-06) เพื่อรองรับ **C8** | สูง |
| FR-16 | Export หลักฐาน AUN-QA | ผู้ประสาน AUN-QA สามารถ export ข้อมูลสถิติจาก dashboard เป็นไฟล์ เพื่อใช้เป็นหลักฐานประกอบการประเมิน Criterion 6 (อ้างอิง C5, C8) โดยไม่กำหนดรูปแบบไฟล์เฉพาะจนกว่าจะตัดสินใจ technology stack — สำหรับหลักฐานเชิงลึกต่อรายเคส dropout (reason code + timeline + referral + resolution) ดู FR-28 ซึ่งเป็นการ export ระดับรายเคส แยกจาก FR-16 ที่เป็น export สถิติรวมระดับ dashboard | สูง |
| FR-17 | ดูสถานะ/ประวัติคำขอของตนเอง (นักศึกษา) | นักศึกษาดูสถานะคำขอของตนเอง (ยื่นแล้ว/รอนัด/นัดแล้ว/เสร็จสิ้น) และประวัติการพบที่ผ่านมาได้ | กลาง |
| FR-18 | ดูรายการคำขอ/ประวัติการพบ (อาจารย์ที่ปรึกษา) | อาจารย์ที่ปรึกษาดูรายการคำขอและประวัติการพบของนักศึกษาในความรับผิดชอบของตนเองได้ — รวมถึง timeline ประวัติการพบรายคนแบบเต็ม (ดู FR-28) สำหรับนักศึกษาในความรับผิดชอบของตนเอง | สูง |
| FR-19 | จัดการสิทธิ์ผู้ใช้ตามบทบาท | Admin จัดการบทบาทผู้ใช้ (Student, Advisor, Program Chair/QA Coordinator, Admin) และสิทธิ์การเข้าถึงข้อมูลตามบทบาท | สูง |
| FR-20 | ควบคุมการเข้าถึงบันทึกที่มีข้อมูลอ่อนไหว | ระบบจำกัดการเข้าถึงเนื้อหาบันทึกการพบที่ถูกระบุว่าอ่อนไหว (เช่น กรณีส่งต่อสุขภาพจิต) ให้เฉพาะผู้ที่เกี่ยวข้องโดยตรงเท่านั้น ผู้ประสาน AUN-QA เห็นได้เฉพาะข้อมูลสถิติ ไม่เห็นเนื้อหาบันทึกแบบละเอียด — **ยืนยันแล้วตามสมมติฐานเดิม** (ดูหัวข้อ 7.3) — ข้อจำกัดนี้ครอบคลุม reason code ที่อ่อนไหว (เช่น สุขภาพใจ) ด้วยเช่นกันเมื่อแสดง/export ให้ผู้ประสาน AUN-QA ตาม FR-15/FR-16 (แสดงได้เฉพาะระดับนับจำนวน ไม่ใช่เนื้อหาบันทึกดิบรายบุคคล) — **ข้อยกเว้นแบบจำกัดขอบเขต (scoped carve-out) — ยืนยันแล้ว (ดูหัวข้อ 7.5):** หัวหน้าสาขา/ผู้ประสาน AUN-QA เข้าถึงข้อมูล case-level เต็มได้ **เฉพาะเคสที่ถูกระบุเป็น dropout/ลาออก/พักการศึกษาเท่านั้น** (ตาม FR-28) เพื่อวัตถุประสงค์การวิเคราะห์เชิงคุณภาพ C8 โดยเฉพาะ ไม่ใช่การเปิดกว้างให้เห็นบันทึกอ่อนไหวของเคสทั่วไปอื่นๆ (เคสที่ไม่ใช่ dropout ยังคงเห็นได้เฉพาะสถิติรวมตามหลักการเดิม) และการเข้าถึงระดับ case-level ทุกครั้งต้องถูกบันทึก access audit log (ดู NFR-07) | สูง |
| FR-21 | กำหนด "วิธีลงนามที่อนุญาต" ต่อประเภทเรื่อง/เอกสาร | แต่ละประเภทเรื่อง/ประเภทเอกสารมี attribute ระบุวิธีลงนามที่อนุญาต 2 ค่า: `wet-signature-only` (ต้องเซ็นจริงแล้วอัปโหลดสแกนตาม FR-11 เท่านั้น) หรือ `e-signature-allowed` (ใช้ e-signature ในระบบได้ตาม FR-12) ระบบใช้ attribute นี้เพื่อเสนอ flow การลงนามที่ถูกต้องให้ผู้ใช้โดยอัตโนมัติ ต่อยอดจากการตั้งค่าประเภทเรื่องใน FR-02/FR-03 (เพิ่มใหม่จากการยืนยันโมเดลลายเซ็นแบบผสม — ดูหัวข้อ 7.1) | สูง |
| FR-22 | นำเข้าและดูแล roster นักศึกษา–อาจารย์ที่ปรึกษา | Admin นำเข้า (import) รายชื่อนักศึกษาและความสัมพันธ์กับอาจารย์ที่ปรึกษาจากไฟล์ Excel/CSV ที่ export มาจากระบบ reg (เช่น ต้นภาคการศึกษา/ต้นเทอม) เนื่องจากระบบ reg ไม่เปิดให้เชื่อมต่อโดยตรง และสามารถแก้ไขรายบุคคลด้วยมือได้ (เพิ่ม/ย้าย/ปิดใช้งานความสัมพันธ์) เพื่อรองรับการเปลี่ยนแปลงกลางภาค (เพิ่มใหม่จากการยืนยันว่าเชื่อมต่อ reg ไม่ได้ — ดูหัวข้อ 7.2) | สูง |
| FR-23 | ชุดข้อมูลมาตรฐานของบันทึกการเข้าพบ (structured consultation record) | ระบบต้องเก็บชุดข้อมูลอย่างน้อยต่อไปนี้ต่อ 1 คำขอ/การพบ 1 รายการ โดยออกแบบเป็น coded/enumerated value ให้มากที่สุด (ไม่ใช่ free text ล้วน) เพื่อให้ **aggregate/จัดกลุ่ม/หา trend** ได้ตามหลัก AUN-QA: (1) รหัสนักศึกษา, ชื่อ, ชั้นปี, ปีหลักสูตร/รหัสรุ่น (curriculum year/cohort) — อ้างอิงจาก roster ภายในระบบ (FR-04/FR-22) ไม่ใช่จากระบบ reg โดยตรง ใช้ผูก cohort เพื่อ progress monitoring (**C6.2**); (2) ชื่ออาจารย์ที่ปรึกษา — อ้างอิงจาก roster เดียวกัน ใช้พิสูจน์ว่าความสัมพันธ์ advisor–advisee ทำงานจริง (**C6.3**); (3) ประเภทเรื่อง (coded ตาม FR-02) — ฟิลด์สำคัญที่สุดสำหรับการ aggregate; (4) ชนิดเอกสารที่ขอลายเซ็น (coded) — เชื่อมกับวิธีลงนามที่อนุญาต (FR-21) และ path การลงนามจริง (FR-11/FR-12) ใช้วัดภาระงาน/administrative support (**C5**); (5) วันที่ยื่นคำขอ และวันที่พบจริง (timestamp บันทึกอัตโนมัติ) — ใช้วัด responsiveness และความถี่ของการพบ (เชื่อมกับ FR-14, NFR-05) | สูง |
| FR-24 | สถานะวงจรคำขอ (status lifecycle) | ระบบกำหนด state model ของคำขอเข้าพบแต่ละรายการเป็นลำดับที่ชัดเจน: ขอ (Requested) → นัดแล้ว (Scheduled) → พบแล้ว (Met) → เซ็น/เสร็จ (Signed/Closed) พร้อมรองรับสถานะยกเลิก (Cancelled) ทุกการเปลี่ยนสถานะบันทึก timestamp โดยอัตโนมัติ เพื่อใช้วัด turnaround time ต่อขั้นตอน และป้องกันไม่ให้มีคำขอตกหล่นโดยไม่มีการอัปเดตสถานะ — เป็น state model ของ record ที่แยกจาก FR-14 (ซึ่งวัดเวลาตอบสนองรวมยื่น→ได้นัด) แต่ใช้ timestamp ของแต่ละสถานะร่วมกัน — การกระทำที่อาจารย์เลือกผ่านลิงก์ตอบกลับในอีเมล (FR-26) เป็นหนึ่งในช่องทางที่ทำให้สถานะเปลี่ยน (เช่น "รับเรื่อง"/"เสนอวันเวลานัด" ทำให้เปลี่ยนจาก ขอ → นัดแล้ว) เทียบเท่ากับการกระทำในหน้าระบบโดยตรง | สูง |
| FR-25 | บันทึกความยินยอม PDPA (consent) ตอนยื่นคำขอ | ระบบบังคับให้นักศึกษาให้ความยินยอม (checkbox บังคับ ไม่สามารถยื่นคำขอต่อได้หากไม่ติ๊ก) ก่อนยื่นคำขอเข้าพบทุกครั้ง โดยบันทึก timestamp ที่ให้ความยินยอม และเวอร์ชันของข้อความ consent ที่ใช้ ณ เวลานั้นไว้เป็นหลักฐาน — เป็นการ capture ข้อมูลเชิง functional ที่ผูกกับนโยบาย PDPA ใน NFR-06 (ซึ่งเป็นข้อกำหนดเชิงไม่เชิงฟังก์ชัน/policy) | สูง |
| FR-26 | ส่งอีเมลสรุปคำขอพร้อมลิงก์ตอบกลับแบบปลอดภัยไปยังอาจารย์ที่ปรึกษาเมื่อ route คำขอสำเร็จ | เมื่อระบบ route คำขอไปยังอาจารย์ที่ปรึกษาที่ถูกต้องแล้ว (ตาม FR-05) ระบบต้องส่งอีเมลสรุปคำขอไปยังอาจารย์ที่ปรึกษาโดยอัตโนมัติ ประกอบด้วยสาระสำคัญของคำขอ (ชื่อนักศึกษา, ประเภทเรื่องตาม FR-02, ชนิดเอกสารที่ขอลายเซ็นตาม FR-21/FR-23, ข้อความ/เหตุผลที่นักศึกษาระบุ) พร้อมแนบ "ลิงก์ตอบกลับ" (actionable link) ที่พาอาจารย์เข้าสู่ระบบเพื่อดำเนินการกับคำขอได้ทันที (เช่น รับเรื่อง, เสนอวันเวลานัด, ปฏิเสธ) — เป็น actionable link ที่นำเข้าสู่ระบบ **ไม่ใช่การ reply อีเมลแบบ parse ข้อความ** ลิงก์ต้องยืนยันตัวตนเฉพาะอาจารย์ผู้รับผิดชอบคำขอนั้น มีอายุจำกัดและ/หรือใช้ได้ครั้งเดียว (tokenized/expiring link — ดู NFR-01) เพื่อป้องกันผู้อื่นตอบแทน การกระทำใดๆ ที่อาจารย์เลือกผ่านลิงก์นี้ต้องถูกบันทึกเป็น log ในระบบ (ผู้กระทำ, การกระทำที่เลือก, timestamp การเปิด/ตอบ) เพื่อเป็นหลักฐานว่ามีการตอบสนองจริง (ผูกกับ NFR-05) หากการกระทำทำให้สถานะคำขอเปลี่ยน ต้องสะท้อนไปยัง state model ใน FR-24 ด้วย รองรับ **C6.3** (หลักฐาน responsiveness ของการให้คำปรึกษา) — ไม่ผูกผู้ให้บริการอีเมลหรือรูปแบบ URL ที่เฉพาะเจาะจง รอการตัดสินใจในขั้น technology-stack | สูง |
| FR-27 | เก็บ reason code / root cause ของการออกกลางคัน (dropout) และการลาออก/พักการศึกษา | ระบบเก็บ "รหัสสาเหตุ" (reason code) เป็น **coded taxonomy** (ไม่ใช่ free text ล้วน) — เป็นฟิลด์ **บังคับ (mandatory)** เมื่อคำขอถูกจัดประเภทเรื่องเป็น "ลาออก/พักการศึกษา" (FR-02 ข้อ 6) หรือถูกระบุว่าเป็นกรณี dropout taxonomy ตั้งต้นที่ Admin ตั้งค่าให้: (1) การเงิน, (2) ผลการเรียน (probation/GPA), (3) สุขภาพกาย, (4) สุขภาพใจ, (5) ปัญหาครอบครัว/ส่วนตัว, (6) ย้ายสถาบัน/เปลี่ยนสาขา, (7) ปัญหาการปรับตัว/สังคม, (8) เหตุผลด้านอาชีพ/งาน, (9) อื่นๆ — Admin ขยาย/แก้ไข taxonomy นี้ได้ในแนวเดียวกับการจัดการประเภทเรื่อง (ดู FR-03) มีช่องขยายความ (free text) ประกอบ reason code ได้ แต่ตัว code ต้องเลือกจากรายการเสมอ เพื่อให้ aggregate/หา pattern ได้ตามหลัก AUN-QA รองรับ **C8** (retention/dropout) เชื่อมกับ **C6.2** (monitoring student progress) — ระบบเก็บ field นี้ให้มนุษย์นำไปวิเคราะห์เอง **ไม่จัด theme อัตโนมัติหรือใช้ NLP วิเคราะห์** (นอกขอบเขตของรอบนี้) | สูง |
| FR-28 | รวบรวมประวัติการเข้าพบรายคน (consultation timeline/case history) และ export ชุดหลักฐานเคส dropout | ระบบรวบรวม/เชื่อมโยงประวัติการเข้าพบทั้งหมดของนักศึกษารายคน (จากข้อมูลที่มีอยู่แล้วตาม FR-08, FR-09, FR-23) เป็น timeline เดียวต่อนักศึกษา 1 คน เพื่อใช้ทบทวนเชิงคุณภาพ และสามารถ export ชุดหลักฐานเคส dropout ต่อรายบุคคล ประกอบด้วย reason code (FR-27) + timeline การพบ + interaction narrative (FR-08) + referral (FR-10) + resolution (FR-09) เป็นไฟล์หลักฐานสำหรับ **C8** — ระบบทำหน้าที่เพียง**รวบรวม/เชื่อมโยง/ส่งออกข้อมูลเท่านั้น ไม่วิเคราะห์ theme หรือสังเคราะห์ narrative ให้อัตโนมัติ** (ไม่ใช่โมดูล qualitative analysis/NLP — นอกขอบเขตของรอบนี้ตามที่ยืนยันแล้ว) **สิทธิ์การเข้าถึง (ยืนยันแล้ว — ดูหัวข้อ 7.5):** (1) อาจารย์ที่ปรึกษาเจ้าของเคสเข้าถึง timeline/narrative เต็มของนักศึกษาในความรับผิดชอบได้เสมอ (ตาม FR-18); (2) หัวหน้าสาขา/ผู้ประสาน AUN-QA เข้าถึง timeline เต็ม + interaction narrative + reason code (รวมค่าที่อ่อนไหว) และ export ชุดหลักฐานเคสได้ **เฉพาะเคสที่ถูกระบุเป็น dropout/ลาออก/พักการศึกษาเท่านั้น** (ไม่ใช่ทุกคำขอ/ทุกการพบ — data minimization) เพราะจำเป็นต่อการเขียนวิเคราะห์เชิงคุณภาพ AUN-QA C8 — สำหรับเคสที่ไม่ใช่ dropout ผู้ประสาน AUN-QA ยังเห็นได้เฉพาะสถิติรวมตาม FR-20/NFR-06 เช่นเดิม ไม่ขยายสิทธิ์เกินขอบเขตนี้; (3) ทุกครั้งที่หัวหน้าสาขา/ผู้ประสาน AUN-QA เปิดดูหรือ export เนื้อหา case-level ของเคส dropout ต้องถูกบันทึกลง access audit log (ใคร/เมื่อไร/เคสใด/การกระทำ — ดู NFR-07) — ไม่ผูก tech stack/รูปแบบไฟล์เฉพาะ | สูง |

## 5. ความต้องการไม่เชิงฟังก์ชัน (Non-Functional Requirements)

| รหัส | ด้าน | ความต้องการ |
|---|---|---|
| NFR-01 | Security & Access Control | ระบบต้องควบคุมสิทธิ์การเข้าถึงข้อมูลตามบทบาทผู้ใช้ (role-based access control) โดยเฉพาะบันทึกการพบที่มีข้อมูลอ่อนไหว — **ปรับเพิ่ม (เกี่ยวกับ FR-26):** ลิงก์ตอบกลับที่ส่งไปทางอีเมลนอกระบบ (actionable link) ต้องยืนยันตัวตนเฉพาะบุคคล มีอายุจำกัดและ/หรือใช้ได้ครั้งเดียว (tokenized/expiring) เพื่อป้องกันไม่ให้ผู้อื่นที่ไม่ใช่อาจารย์ที่ปรึกษาเจ้าของคำขอตอบแทนได้ |
| NFR-02 | Data Integrity | **แก้ไขหลังยืนยันว่าเชื่อมต่อ reg ไม่ได้ (ดูหัวข้อ 7.2):** ระบบถือสำเนา (roster) ข้อมูลนักศึกษา–อาจารย์ที่ปรึกษาที่นำเข้าจากไฟล์ reg export (FR-22) ต้องมีกลไกกำกับความสดของข้อมูล (data freshness) เช่น บันทึกวันที่ import ล่าสุดและภาคการศึกษาที่อ้างอิงไว้กับ roster ทุกครั้ง และรองรับการ re-import ทับเพื่ออัปเดตข้อมูล โดยต้องคงประวัติการพบ (interaction note) เดิมของนักศึกษาไว้ไม่ให้สูญหายเมื่อ re-import |
| NFR-03 | Auditability | **ปรับให้ครอบคลุมโมเดลลายเซ็นแบบผสม (ดูหัวข้อ 7.1):** ทุกการอนุมัติ/ลงลายมือชื่อต้องมี timestamp และ audit trail (ผู้กระทำ, เวลา, การกระทำ) ที่ตรวจสอบย้อนหลังได้ ครอบคลุมทั้ง 2 path คือ (1) เอกสารที่เซ็นจริงบนกระดาษแล้วอัปโหลดสแกน (FR-11) และ (2) เอกสารที่ลงนามด้วย e-signature ในระบบ (FR-12) โดยต้องบันทึกด้วยว่าเอกสารแต่ละฉบับใช้ path ใด ตาม attribute วิธีลงนามที่อนุญาต (FR-21) |
| NFR-04 | Reportability | ระบบต้องสามารถ export ข้อมูลสถิติที่ dashboard แสดงผลเป็นไฟล์ที่ใช้เป็นหลักฐานประกอบการประเมิน AUN-QA ได้ โดยไม่ผูกรูปแบบไฟล์จนกว่าจะมีการตัดสินใจ technology stack |
| NFR-05 | Traceability of Time-based Metrics | ทุก event ที่เกี่ยวข้องกับเวลาตอบสนอง (ยื่นคำขอ, นัดหมาย, ยืนยันนัด) ต้องถูกบันทึกเวลาโดยอัตโนมัติ เพื่อให้คำนวณเวลาตอบสนองได้แม่นยำและตรวจสอบย้อนหลังได้ — **ปรับเพิ่ม (เกี่ยวกับ FR-26):** ครอบคลุมการตอบสนองผ่านลิงก์ตอบกลับในอีเมลด้วย (เวลาที่เปิดลิงก์/เวลาที่เลือกการกระทำ) ไม่ใช่เฉพาะการกระทำในหน้าระบบเท่านั้น |
| NFR-06 | Privacy / PDPA Compliance | การเก็บ เข้าถึง และเก็บรักษาบันทึกการพบที่เป็นข้อมูลส่วนบุคคล/ข้อมูลอ่อนไหว ต้องสอดคล้องกับ PDPA — **ยืนยันแล้วว่าผู้ประสาน AUN-QA เห็นได้เฉพาะข้อมูลสถิติรวม ไม่เห็นเนื้อหาบันทึกรายบุคคล (ดูหัวข้อ 7.3)** ส่วนระยะเวลา retention ที่ชัดเจนยังไม่ได้ข้อสรุป (คงเป็นประเด็นเปิดที่เหลืออยู่ ต้องกำหนดก่อนขึ้น production) — ครอบคลุม reason code ที่อ่อนไหว (FR-27 เช่น สุขภาพใจ) และชุดหลักฐานเคส dropout (FR-28) ด้วยเช่นกัน — **ข้อยกเว้นแบบจำกัดขอบเขต (scoped carve-out) — ยืนยันแล้ว (ดูหัวข้อ 7.5):** สำหรับเคสที่ถูกระบุเป็น dropout/ลาออก/พักการศึกษาเท่านั้น หัวหน้าสาขา/ผู้ประสาน AUN-QA เข้าถึงเนื้อหา case-level เต็มได้ (รวมค่าที่อ่อนไหว) โดย **lawful basis/ความจำเป็นเชิงประเมินคุณภาพ** คือการวิเคราะห์ root cause การออกกลางคันตาม AUN-QA Criterion 8 (purpose limitation — ใช้ได้เฉพาะวัตถุประสงค์นี้ ไม่ใช่เพื่อการอื่น) เคสที่ไม่ใช่ dropout ยังคงอยู่ภายใต้กฎเดิมของ FR-20 (สถิติรวมเท่านั้น) ทุกการเข้าถึง case-level ต้องมี access audit log (ดู NFR-07) |
| NFR-07 | Access/Read Audit Logging (การบันทึกการเข้าถึง/อ่านข้อมูล) | ทุกครั้งที่หัวหน้าสาขา/ผู้ประสาน AUN-QA เปิดดูหรือ export เนื้อหา case-level เต็ม (timeline + interaction narrative + reason code) ของเคส dropout/ลาออก/พักการศึกษา (ตาม FR-20/FR-28) ระบบต้องบันทึก access audit log แยกจาก NFR-03 ซึ่งเป็น audit trail ของการอนุมัติ/ลงลายมือชื่อ (write/approval audit) — NFR-07 นี้คือ **read/access audit** โดยเฉพาะ บันทึกอย่างน้อย: ผู้เข้าถึง (ใคร), เวลาที่เข้าถึง (เมื่อไร), เคส/นักศึกษาที่ถูกเข้าถึง (เคสใด), และการกระทำ (ดู/export) log นี้ต้องตรวจสอบย้อนหลังได้ เพื่อเป็นหลักฐานว่าการเข้าถึงข้อมูลอ่อนไหวเกิดขึ้นตามขอบเขตที่กำหนด (จำกัดเฉพาะเคส dropout) เท่านั้น |

## 6. ความเชื่อมโยงกับ AUN-QA

| Criterion | เชื่อมโยงกับ FR/NFR |
|---|---|
| C6.2 (monitoring student progress) | FR-08, FR-09, FR-15, FR-16, FR-23 (cohort/ชั้นปี), FR-24 (ไม่ให้เคสตกหล่น), FR-27 (root cause เชื่อมกับ progress monitoring), NFR-05 |
| C6.3 (advising availability & quality) | FR-01, FR-02, FR-05, FR-06, FR-07, FR-08, FR-13, FR-14, FR-15, FR-23 (ยืนยันความสัมพันธ์ advisor–advisee), FR-24 (turnaround), FR-26 (หลักฐานการตอบสนองของอาจารย์ผ่านลิงก์อีเมล) |
| C5 (staff workload) | FR-15 (workload ต่ออาจารย์), FR-16, FR-23 (ชนิดเอกสาร/administrative support) |
| C8 (retention/dropout) | FR-09 (referral/follow-up), FR-10, FR-16, FR-02 (จัดกลุ่มประเภทเรื่องเพื่อวิเคราะห์ trend เช่น ลาออกเชื่อมกับการเงิน), FR-27 (reason code/root cause ของ dropout), FR-28 (timeline + case-level access + export หลักฐานเคส dropout รายบุคคล — รวมสิทธิ์ QA Coordinator แบบ scoped carve-out), FR-20/NFR-06 (ขอบเขตการเข้าถึงข้อมูลอ่อนไหวแบบ carve-out เฉพาะเคส dropout), **NFR-07 (access audit log การเข้าถึง case-level ของเคส dropout — เพิ่มใหม่)** |

## 7. ผลการยืนยันประเด็นเปิด (Resolved — ยืนยันโดยผู้ใช้ เมื่อ 2026-08-26)

เอกสารฉบับร่างแรกตั้งสมมติฐานไว้ 3 ประเด็นที่ raw requirement ยังไม่ระบุชัด ผู้ใช้ได้ยืนยันครบทั้ง 3 ข้อแล้ว
สรุปผลและรายการ FR/NFR ที่ได้รับผลกระทบไว้ดังนี้:

### 7.1 โมเดลลายเซ็น — ยืนยันแล้ว: แบบผสม (Hybrid)

**ผลการยืนยัน:** เอกสารจำนวนมากยังต้องเซ็นจริงบนกระดาษ (wet signature) แล้วอัปโหลดสแกน ส่วนบางประเภทเอกสาร
ใช้ e-signature ในระบบได้ — ไม่ใช่ "อย่างใดอย่างหนึ่ง" ตามสมมติฐานเดิม แต่เป็นการ**ใช้ทั้งสองแบบร่วมกัน**
โดยแยกตามประเภทเอกสาร

**ผลกระทบต่อเอกสาร:**
- FR-11 (อัปโหลดสแกน + audit trail) — คงเป็น**สูง/MVP** เป็น path หลักสำหรับเอกสารที่ต้องเซ็นจริง
- FR-12 (e-signature ในระบบ) — คงระดับ**กลาง** ตามเดิม แต่ระบุชัดว่าใช้ได้เฉพาะประเภทเอกสารที่อนุญาตเท่านั้น
- **FR-21 (ใหม่)** — attribute "วิธีลงนามที่อนุญาต" (`wet-signature-only` / `e-signature-allowed`) ต่อประเภทเรื่อง/เอกสาร
- NFR-03 — ปรับให้ audit trail ครอบคลุมทั้ง 2 path (สแกนเอกสารเซ็นจริง และ e-signature)

### 7.2 การเชื่อมต่อกับระบบ reg — ยืนยันแล้ว: เชื่อมต่อไม่ได้

**ผลการยืนยัน:** ระบบทะเบียนของมหาวิทยาลัยไม่เปิดให้เชื่อมต่อ (ไม่มี API และไม่ integrate) สมมติฐานเดิม
("reg เป็น source of truth เดียว / ไม่สร้างฐานข้อมูลนักศึกษาซ้ำ") **ใช้ไม่ได้** — ระบบ AdvisingLog ต้องดูแล
roster นักศึกษา–อาจารย์ที่ปรึกษาของตัวเอง

**ผลกระทบต่อเอกสาร:**
- FR-04 — เปลี่ยนจาก "ยืนยันข้อมูลจาก reg แบบ integrate" เป็น "จัดเก็บ roster นักศึกษา–อาจารย์ที่ปรึกษา
  ภายในระบบเอง" ยืนยันความสัมพันธ์จาก roster ภายใน ไม่ใช่จาก reg
- **FR-22 (ใหม่)** — นำเข้า (import) และดูแล roster นักศึกษา–อาจารย์ที่ปรึกษา: รองรับ import ไฟล์ Excel/CSV
  (ต้นเทอม/รายภาค) + แก้ไขด้วยมือรายบุคคล (เพิ่ม/ย้าย/ปิดใช้งาน) โดย Admin
- FR-05 (auto-route) — ปรับให้อ้างอิง roster ภายในระบบ (จากการ import) แทนการ query reg
- NFR-02 — แก้จาก "reg เป็น source of truth เดียว / ห้ามสร้าง DB ซ้ำ" เป็น "ระบบถือสำเนา roster ที่ import มา
  ต้องมีกลไกกำกับความสดของข้อมูล (data freshness) และรองรับการ re-import ทับเพื่ออัปเดต โดยคงประวัติการพบเดิมไว้"
- Scope (หัวข้อ 2.1/2.2) และ Actors (หัวข้อ 3) ปรับข้อความที่เคยเขียนว่า integrate/ดึง real-time จาก reg
  ทั้งหมดให้สะท้อนว่าใช้ roster ภายในระบบแทน — ตรวจแล้วไม่มีข้อความ integrate/real-time หลงเหลืออยู่ในเอกสารนี้

### 7.3 PDPA และการเข้าถึงบันทึกการพบที่อ่อนไหว — ยืนยันแล้ว: ตามสมมติฐานเดิม

**ผลการยืนยัน:** หัวหน้าสาขา/ผู้ประสาน AUN-QA เห็นเฉพาะข้อมูลสถิติรวม (aggregate) ไม่เห็นเนื้อหาบันทึกการพบ
รายบุคคลที่อ่อนไหว ตรงกับสมมติฐานเดิมทุกประการ

**ผลกระทบต่อเอกสาร:** ไม่มีการแก้ไขเชิงสาระ — FR-20 และ NFR-06 ถูกต้องแล้วตามที่เขียนไว้ในร่างแรก
(ปรับเฉพาะข้อความให้ระบุว่า "ยืนยันแล้ว" แทนคำว่า "สมมติฐาน")

### 7.4 ขอบเขตการวิเคราะห์ root cause ของ dropout — ยืนยันแล้ว: ขอบเขต A เท่านั้น

**ผลการยืนยัน:** ผู้ใช้เลือกขอบเขต **A — "ระบบเป็นแหล่งหลักฐาน"** คือเก็บ reason code (coded taxonomy)
+ เชื่อมโยงประวัติการพบ + export ให้มนุษย์นำไปวิเคราะห์เอง โดย**ไม่เลือก** (B) โมดูลบันทึก/สังเคราะห์
qualitative narrative ในระบบ และ**ไม่เลือก** (C) ระบบช่วยจัด theme อัตโนมัติ/NLP — ทั้งสองแนวทางนี้
**อยู่นอกขอบเขตของรอบนี้โดยชัดแจ้ง** ไม่ถูกใส่เข้ามาใน FR-27/FR-28 หรือ FR อื่นใดในเอกสารนี้

**ผลกระทบต่อเอกสาร:**
- **FR-27 (ใหม่)** — เก็บ reason code เป็น coded taxonomy (mandatory เมื่อเป็นเคสลาออก/พักการศึกษา/dropout)
- **FR-28 (ใหม่)** — รวบรวม timeline ประวัติการพบรายคน + export ชุดหลักฐานเคส dropout (รวบรวม/เชื่อมโยง/
  ส่งออกเท่านั้น ไม่วิเคราะห์)
- ทั้ง FR-27 และ FR-28 เขียนไว้ชัดเจนว่า **"ไม่จัด theme อัตโนมัติหรือใช้ NLP วิเคราะห์"** และ
  **"ไม่วิเคราะห์ theme หรือสังเคราะห์ narrative ให้อัตโนมัติ"** ตามลำดับ เพื่อกันไม่ให้ scope คืบไปทาง B/C
  โดยไม่ตั้งใจในการออกแบบ/พัฒนาขั้นถัดไป
- หากในอนาคตต้องการขยายไปทำ B หรือ C จะต้องเปิดเป็นรอบ requirement ใหม่ พร้อมยืนยันขอบเขตกับผู้ใช้อีกครั้ง

### 7.5 สิทธิ์การเข้าถึงข้อมูลเชิงคุณภาพของเคส dropout — ยืนยันแล้ว: ข้อ 3 (scoped carve-out เฉพาะเคส dropout)

**ผลการยืนยัน:** ผู้ใช้เลือกข้อ 3 จาก 3 ตัวเลือก — หัวหน้าสาขา/ผู้ประสาน AUN-QA เห็นข้อมูล case-level เต็ม
(timeline การพบ + interaction narrative + reason code รวมค่าที่อ่อนไหว) และ export ชุดหลักฐานเคสได้
**เฉพาะเคสที่เป็น dropout/ลาออก/พักการศึกษาเท่านั้น** เพราะจำเป็นต่อการเขียนวิเคราะห์เชิงคุณภาพ AUN-QA C8
โดยต้องมี access audit log บันทึกการเข้าถึง — **ยืนยันชัดเจนว่าขอบเขตการผ่อนปรนนี้จำกัดเฉพาะเคส dropout
เท่านั้น ไม่ใช่การเปิดกว้างสิทธิ์ QA Coordinator ให้เห็นบันทึกอ่อนไหวของเคสทั่วไปทุกเคส** — สำหรับเคสที่ไม่ใช่
dropout หลักการเดิมใน FR-20/NFR-06 (เห็นเฉพาะสถิติรวม) ยังคงมีผลเหมือนก่อนหน้านี้ทุกประการ

**ผลกระทบต่อเอกสาร:**
- **FR-28** — ขยายสิทธิ์ผู้เข้าถึง: เดิมกำหนดไว้ว่า "การดูรายละเอียดเต็มจำกัดเฉพาะอาจารย์ที่ปรึกษาเจ้าของเคส"
  → เพิ่มหัวหน้าสาขา/ผู้ประสาน AUN-QA เข้าถึง timeline เต็ม + narrative + export ได้ **เฉพาะเคส dropout**
  (data minimization — ไม่ใช่ทุกคำขอ/ทุกการพบ)
- **FR-20** — เพิ่มข้อยกเว้นแบบจำกัดขอบเขต (scoped carve-out) ระบุชัดว่าเป็นข้อยกเว้นเฉพาะเคส dropout
  เท่านั้น หลักการเดิม (สถิติรวมเท่านั้น) ยังใช้กับเคสทั่วไป
- **NFR-06** — เพิ่มข้อความเดียวกัน พร้อมระบุ lawful basis/ความจำเป็นเชิงประเมินคุณภาพ (จำเป็นสำหรับการ
  วิเคราะห์ root cause ตาม AUN-QA C8) และ purpose limitation (ใช้ได้เฉพาะวัตถุประสงค์นี้)
- **NFR-07 (ใหม่)** — access/read audit logging: บันทึกทุกครั้งที่หัวหน้าสาขา/ผู้ประสาน AUN-QA เปิดดู/
  export เนื้อหา case-level ของเคส dropout (ใคร/เมื่อไร/เคสใด/การกระทำ) แยกจาก NFR-03 ที่เป็น audit
  ของการอนุมัติ/ลงลายมือชื่อ (write/approval audit) โดยเจตนา เพื่อความชัดเจนของ traceability ระหว่าง
  "ใครอนุมัติอะไร" (NFR-03) กับ "ใครอ่าน/เห็นข้อมูลอ่อนไหวอะไรบ้าง" (NFR-07)

## 8. Mapping ฟิลด์ระดับ AUN-QA (2026-08-26 — เพิ่มตามรายละเอียดฟิลด์ที่ผู้ใช้ให้มา)

ผู้ใช้ได้ให้รายละเอียดฟิลด์ระดับ AUN-QA ที่ต้องเก็บ พร้อมเหตุผล (AUN-QA angle) ของแต่ละฟิลด์ โดยย้ำว่า
หัวใจของ AUN-QA ไม่ใช่แค่ "มีบันทึก" แต่ต้อง **aggregate + วิเคราะห์ + close the loop ได้** ตารางด้านล่าง
สรุปว่าแต่ละฟิลด์ที่ผู้ใช้ระบุ ถูก cover ด้วย FR ใดในเอกสารนี้ (ไม่มีฟิลด์ใดตกหล่น):

| ฟิลด์ที่ผู้ใช้ระบุ | AUN-QA angle | FR ที่ cover |
|---|---|---|
| Student ID, ชื่อ, ชั้นปี, ปีหลักสูตร (curriculum year) | ผูกกับ cohort เพื่อ progress monitoring (6.2) | FR-23 (อ้างอิงจาก roster ตาม FR-04/FR-22) |
| ชื่ออาจารย์ที่ปรึกษา | พิสูจน์ว่าระบบ advisor–advisee ทำงานจริง (6.3) | FR-23 (อ้างอิงจาก roster ตาม FR-04/FR-22), FR-05 |
| วันที่ขอเข้าพบ / วันที่พบจริง | responsiveness + ความถี่ของการพบ = หลักฐานว่า advising เกิดจริง | FR-23 (เก็บ timestamp), FR-14 (คำนวณเวลาตอบสนอง), NFR-05 |
| ประเภทเรื่อง (dropdown บังคับ) | สำคัญที่สุด ทำให้ aggregate ได้ | FR-02 (dropdown บังคับ 9 ประเภท + sub-type) |
| เอกสารที่ขอลายเซ็น (ระบุชนิด) | ผูกกับ workload/administrative support | FR-23 (ฟิลด์ชนิดเอกสาร), FR-21, FR-11/FR-12 |
| สถานะ (ขอ → นัดแล้ว → พบแล้ว → เซ็น/เสร็จ) | วัด turnaround, ไม่มีเคสตกหล่น | FR-24 (status lifecycle) |
| บันทึกของอาจารย์ / action ที่ทำ | หลักฐานเชิงคุณภาพของ counselling (6.3) | FR-08 (interaction note) |
| ต้อง follow-up / ส่งต่อหน่วยงานไหม (referral) | close the loop (ทุน, แนะแนว, หน่วยการเงิน) | FR-09 (follow-up), FR-10 (referral) |
| ผลลัพธ์ / resolution | ปิดเคส + ใช้ทำ improvement action | FR-09 (outcome/follow-up) |
| PDPA consent (checkbox) | จำเป็นเพราะเก็บข้อมูลส่วนบุคคล/สุขภาพจิตบางเคส | FR-25 (functional capture), NFR-06 (policy) |
| Reason code / root cause ของ dropout (เพิ่มเมื่อ 2026-08-26) | root cause analysis ของการออกกลางคัน — close the loop ตามหลัก **C8** เชื่อม **C6.2** | FR-27 (coded taxonomy), FR-28 (timeline + export รายเคส) |

## 9. เอกสารที่เกี่ยวข้อง

- [[backlog]] — สรุปรายการ FR/NFR ทั้งหมดของโปรเจกต์
